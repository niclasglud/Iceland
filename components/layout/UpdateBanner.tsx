'use client'
// This component is intentionally empty — update detection is handled
// via useUpdateReady() in ToolsDrawer, which glows the Refresh App button.
export default function UpdateBanner() { return null }
